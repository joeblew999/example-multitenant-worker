node_modules/
dist/
gen/
.vite/
*.tsbuildinfo
*.log
.DS_Store
# Seed manifests contain live session tokens for seeded users — never commit.
# One per scenario: .seed.editorial.json, .seed.remysport.json, etc.
.seed.*.json
